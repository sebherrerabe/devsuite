{
  "name": "@devsuite/shared",
  "version": "0.0.0",
  "description": "Shared types and utilities for DevSuite",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
